version: 2.1
orbs:
  tfsec: mycodeself/tfsec@1.1.0
jobs:  
  tfsec-tests:
    executor: tfsec/default
    steps:
      - checkout
      - tfsec/scan:
          directory: infrastructure
          exclude-checks: GEN001,GCP001,GCP002
  terraform-setup:
    docker:
      - image: hashicorp/terraform
    steps:
      - checkout
      - run:
          name: Set Environment Variables
          command: |
            if [ "${CIRCLE_BRANCH}" == "main" ]; then
              echo "export TF_VAR_ENVIRONMENT=production" >> env.sh
            else
              echo "export TF_VAR_ENVIRONMENT=test" >> env.sh
            fi
            chmod +x env.sh
      - run:
          name: Terraform Init & Validate & Plan
          command: |
            ./env.sh \
              && cd infrastructure \
              && terraform init \
              && terraform validate \
              && terraform plan
      - persist_to_workspace:
          root: ~/project
          paths:
            - .
  terraform-apply:
    docker:
      - image: hashicorp/terraform
    steps:
      - attach_workspace:
          at: ~/project
      - run:
          name: Apply Terraform Infrastructure
          command: cat env.sh && ./env.sh && cd infrastructure && terraform apply -auto-approve
      - persist_to_workspace:
          root: ~/project
          paths:
            - .
  infrastructure-test:
    docker:
      - image: golang:1.21.3
    steps:
      - attach_workspace:
          at: ~/project
      - run:
          name: Install Terraform
          command: chmod +x install_terraform.sh && ./install_terraform.sh
      - run:
          name: Test Infrastructure
          command: |
            cd test \
              && go mod init github.com/wexire/webserver-ec2-module-terraform \
              && go mod tidy \
              && go test
  terraform-destroy:
    docker:
      - image: hashicorp/terraform
    steps:
      - attach_workspace:
          at: ~/project
      - run: 
          name: Destroy Terraform Infrastructure
          command: ./env.sh && cd infrastructure && terraform destroy -auto-approve
  
workflows:
  tf-test-deploy:
    jobs:
      - tfsec-tests
      - terraform-setup
      - approve_apply:
          type: approval
          requires:
            - terraform-setup
      - terraform-apply:
          requires:
            - approve_apply
      - infrastructure-test:
          requires:
            - terraform-apply
      - approve_destroy:
          type: approval
          requires:
            - infrastructure-test
      - terraform-destroy:
          requires:
            - approve_destroy