terraform {
  backend "s3" {
    bucket         = "bucketformyneeds1"
    key            = "webserver/terraform.tfstate"
    access_key     = ""
    secret_key     = ""
    region         = "us-east-1"
    use_lockfile = true
  }
}